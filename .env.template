# -------- WinkWink Environment --------
NODE_ENV=production
TZ=Asia/Kolkata

# Signaling server (Docker internal)
HOST=http://0.0.0.0
PORT=3000
TRUST_PROXY=true

# Logs
LOGS_DEBUG=false
LOGS_COLORS=true

# CORS (only allow your domain)
CORS_ORIGIN='["https://winkwink.tail.imsudip.in"]'
CORS_METHODS='["GET","POST"]'

# Room settings
ROOM_MAX_PARTICIPANTS=2  # just you and her
HOST_PROTECTED=false
HOST_USER_AUTH=false

# JWT
JWT_KEY=winkwink_secret
JWT_EXP=12h

# STUN / TURN
STUN_SERVER_ENABLED=true
STUN_SERVER_URL=stun:stun.l.google.com:19302

TURN_SERVER_ENABLED=true
TURN_SERVER_URL=turn:a.relay.metered.ca:443
TURN_SERVER_USERNAME=e8dd65b92c62d3e36cafb807
TURN_SERVER_CREDENTIAL=uWdWNmkhvyqTEswO

# Redirects & Survey (off, nobody wants that)
SURVEY_ENABLED=false
REDIRECT_ENABLED=false

# API (you probably won’t use it, keep minimal)
API_KEY_SECRET=winkwink_api_secret
API_DISABLED='["token","meetings"]'

# Disable all the corporate integrations
OIDC_ENABLED=false
NGROK_ENABLED=false
IP_LOOKUP_ENABLED=false
SENTRY_ENABLED=false
SLACK_ENABLED=false
MATTERMOST_ENABLED=false
CHATGPT_ENABLED=false
EMAIL_ALERT=false
STATS_ENABLED=false